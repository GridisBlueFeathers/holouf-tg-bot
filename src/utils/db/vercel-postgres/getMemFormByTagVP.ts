import { sql } from "@vercel/postgres";

const getMemFormByTagVP = async ({ tgTag }: { tgTag: string }) => {

	/*const { rows } = sql`SELECT 
		tg_tag,
		name,
		birthday,
		location`*/

}

export default getMemFormByTagVP;
